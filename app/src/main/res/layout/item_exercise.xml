<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip" >



    <TextView
        android:id="@+id/exercise_timestamp_output"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:maxLines="1"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/exercise_quantity_output"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/exercise_timestamp_output"
        android:maxLines="1"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/exercise_description_output"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/exercise_quantity_output"
        android:maxLines="1"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/exercise_title_output"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/exercise_description_output"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:textSize="16sp" />












</RelativeLayout>